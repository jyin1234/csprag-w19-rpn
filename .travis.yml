language: python
python:
  - "3.5"

install:
  - pip install coverage
  - pip install coverage-badge
  - pip install click
  - pip install termcolor

script:
  - coverage run -m unittest
  - coverage report